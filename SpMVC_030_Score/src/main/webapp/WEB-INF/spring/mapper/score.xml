<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.callor.score.persistance">

	<select id="selectAll" resultType="StudentVO"></select>

	<select id="findById" resultType="StudentVO"></select>

	<insert id="insert" parameterType="StudentVO"></insert>

	<update id="update" parameterType="StudentVO"></update>

	<delete id="delete"></delete>

</mapper> 